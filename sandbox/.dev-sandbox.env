# OpenClaw 开发沙盒配置文件
# 复制为 .dev-sandbox.env.local 可覆盖默认配置

# 镜像配置
OPENCLAW_DEV_IMAGE=openclaw-dev
OPENCLAW_DEV_CONTAINER=openclaw-dev

# 端口映射
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BRIDGE_PORT=18790

# 挂载路径（默认使用当前项目目录和 ~/.openclaw-dev）
# OPENCLAW_PROJECT_DIR=/path/to/your/project
# OPENCLAW_CONFIG_DIR=/path/to/your/.openclaw-dev

# Docker 运行选项
# OPENCLAW_DOCKER_NETWORK=bridge
# OPENCLAW_DOCKER_EXTRA_ARGS="--memory=4g --cpus=2"
